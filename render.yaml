services:
  - type: web
    name: youtube-rag-assistant
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
      - key: ENABLE_RERANKER
        value: "false"
